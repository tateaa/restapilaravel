$kind: http-request
name: Delete Comment
method: DELETE
url: '{{base_url}}/api/posts/{{post_id}}/comments/{{comment_id}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
